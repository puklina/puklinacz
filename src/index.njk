---
layout: base.njk
title: "Puklina – Stavební a rekonstrukční práce"
---

{% include "header.njk" %}

<main>

  {# --- HERO SECTION --- #}
  {% include "hero.njk" %}

  {# --- CLIENT LOGOS --- #}
  <section class="wrapper section--hug logos">
    {% include "logos.njk" %}
  </section>

  {# --- PROJECTS --- #}
  <section class="wrapper section--hug projects" id="projekty">
    <h2>Realizované zakázky</h2>
    <div class="carousel-wrapper">
      <button class="arrow left" aria-label="Předchozí">&#10094;</button>
      <div class="carousel">
        {% for project in collections.projects %}
          {% include "card.njk" %}
        {% endfor %}
      </div>
      <button class="arrow right" aria-label="Další">&#10095;</button>
    </div>
    <div class="center mt-4">
      <a href="https://instagram.com/puklina" target="_blank" class="button">
        Více projektů na Instagramu
      </a>
    </div>
  </section>

  {# --- CONTACT --- #}
  <section class="wrapper section--hug contact" id="kontakt">
    {% include "contact.njk" %}
  </section>

</main>

{% include "footer.njk" %}